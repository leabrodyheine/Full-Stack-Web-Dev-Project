const express = require('express');
const router = express.Router();

// @route   GET api/runs
// @desc    Get all runs
router.get('/', (req, res) => {
  // Handle GET request for runs
});

// Additional routes for POST, PUT, DELETE below...

module.exports = router;
